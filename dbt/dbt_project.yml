name: 'real_estate_analytics_dbt'

version: '1.0.0'

config-version: 2

profile: 'real_estate_analytics_dbt'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"